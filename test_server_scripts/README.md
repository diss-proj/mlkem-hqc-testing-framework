# Test Server Scripts
This directory contains scripts which fix the test
server's frequency and disable runtime dynamic frequency
management features like turbo boost.

Doing this depends heavily on the test platform you're
using so these scripts most likely won't work, but they
should serve as a useful reference.

